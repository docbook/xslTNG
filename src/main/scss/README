These are the SASS sources for the DocBook xslTNG CSS.

They aren't used directly by the stylesheets or in the stylesheet
distribution. They are included because they may form a more useful
starting point than the generated CSS files for extensive
customizations.

